# Build artifacts
engine/build/
**/__pycache__/
*.pyc
*.pyo
*.so

# Training artifacts
*.npz

# Generated visualizations (use record.py to regenerate)
*.gif
*.png
*.ppm

# Reference project — vendored Micropolis source was copied out of
# this into engine/. The original is kept locally for reference only;
# not part of slimcity.
gym-city/

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
.DS_Store

# Local Claude Code settings (per-machine permission allowlist)
.claude/settings.local.json

# Training archives — regenerable, large. The EXPERIMENTS.md / RESULTS.md
# files in the repo describe what each run was. Use `python3 qd_train.py
# --save results/<name>.npz` to reproduce.
results/*.npz
results/*.gif
results/*.png
results/*.log
